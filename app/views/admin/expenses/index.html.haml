%h2= t :expenses
%table
  %thead
    %th Date Paid
    %th Recipient
    %th What For
    %th Amount
    %th Budgetarea
    %th{:colspan => 3} actions
  %tbody
    - for expense in @expenses
      %tr{:class => cycle('odd', 'even')}
        %td= l expense.date_paid
        %td= expense.recipient
        %td
          = expense.what_for
          .other
            = expense.event.try(:name)
            = expense.project.try(:name)
        %td= number_to_currency expense.amount
        %td= expense.budgetarea.try(:name)

        %td= link_to "Show", [:admin, expense]
        %td= link_to "Edit", edit_admin_expense_path(expense)
        %td.destroy= link_to "Destroy", [:admin, expense], :confirm => 'Are you sure?', :method => :delete
= button_to "New expense", new_admin_expense_path, :method => :get
