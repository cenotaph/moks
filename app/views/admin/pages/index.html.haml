- title "Pages"
%table
  %tr
    %th Title
  - for page in @pages
    %tr
      %td= page.title
      %td= link_to "Show", [:admin, page]
      %td= link_to "Edit", edit_admin_page_path(page)
      %td= link_to "Destroy", [:admin,  page], :confirm => 'Are you sure?', :method => :delete
%p= link_to "New page", new_admin_page_path
